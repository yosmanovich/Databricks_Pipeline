bundle:
  name: databricks_app

artifacts:
  python_artifact:
    type: whl
#    build: uv build --wheel
    build: pip install pyspark dlt

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    mode: development
    default: true

    presets:
      artifacts_dynamic_version: true

  prod:
    mode: production
